export interface INewUserSettings {
  userId: string
  currency: string
  monthStart: number
}

export interface IUpdateUserSettings {
  userId: string
  currency: string
  monthStart: number
}

export interface IUserSettings {
  id: string
  userId: string
  currency: string
  monthStart: number
}
