export interface INewUserSettings {
  userId: string
  currency: string
}

export interface IUpdateUserSettings {
  userId: string
  currency: string
}

export interface IUserSettings {
  id: string
  userId: string
  currency: string
}
